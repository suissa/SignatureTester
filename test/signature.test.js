const sinon = require('sinon')
const { expect } = require('chai')
const testSignature = require('../testSignature.js')

describe.skip('Should parse the signature annotation', () => {
	it('should parse the annotation to types')
	it('should parse every annotation to types')
})

describe.skip('Should do in ou test on signatures')
